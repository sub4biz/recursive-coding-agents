---
label: Arbitrary-length context
eyebrow: Use case 1
---

## Context far beyond the window.

Spawn children that programmatically slice the prompt — do work proportional to its size without ever loading it all into one window.

**Reframe for coding:** the "prompt" is every file in your repo.
