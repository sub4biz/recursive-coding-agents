---
label: RLM to recursive coding agent
---

## What makes it *recursive?*

Child calls. A sub-call can be another RLM, recursively — all the way down to the leaf, a plain LLM call.

The environment can be any REPL — Python, or just bash and Unix. That last one is the OpenProse door.
